const GameView = ({controller}) => {
    return (
        <main>
            <form name='act'>
                <input type='text' name='talk'></input>
            </form>
        </main>
    );
}

export default GameView;
